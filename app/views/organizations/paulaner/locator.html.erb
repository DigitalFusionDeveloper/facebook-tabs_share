<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= @brand.title %> Store Locator</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le styles -->
    <%= stylesheet_link_tag("bootstrap") %>

    <style>

      #details {
          position: absolute;
          right: 10px;
          bottom: 0;
          background:rgba(255,255,255,0.8);
          -ms-filter:progid:DXImageTransform.Microsoft.gradient(startColorstr=#26ffffff,endColorstr=#ccffffff);
          filter:progid:DXImageTransform.Microsoft.gradient(startColorstr=#ccffffff,endColorstr=#ccffffff);
          width: 55%;
          padding: 0.5em 1em 1.5em;
      }

      #details p { width: 80%; }

      #paulaner_nav img { width: 112px; }

      /* styles for beer forms */

      .container {
        width: 810px;
      }
      /*
      .container.fb-tab {
        background: url('img/ohara-bg-2.jpg') no-repeat 0 0 !important;
        height: auto;
      }
      */

      #header {margin: 10px 0 10px 0;}

      .form_wrapper {
        background-color: #e7e7e7;
        padding: 20px 0;
      }

      label, .required-field {
          display: inline-block;
      }
      .required-field {
          color: red;
      }

      .btn-primary {
        background: #333;
        text-transform: uppercase;
        margin-top: 10px;
      }

      footer {
        //border-top: 14px solid #cbb572;
        border-top: 14px solid #ccc;
      }

      footer img {
        vertical-align: top;
      }

      .dialog-wrapper {
        background: url('img/paulaner-bg.jpg') no-repeat 0 0 !important;
        height: auto;
      }

      .dialog-pane {
        background-color: rgba(0, 0, 0, .6);
        padding: 20px;
        margin: 25px 0;
        border-radius: 8px;
        border: 1px solid #cbb572;
      }

      .dialog-pane img {
        display: block;
        margin: 0 auto;
      }

      .dialog-pane p {
        margin: 15px 0 0 0;
        color: #fff;
        font-size: 20px;
        line-height: 26px;
        text-align: center;
      }


      .dialog-pane a {
        color: #cbb572;
      }

      .dialog-btn {
        background: #fff;
        border: 1px solid #cbb572;
        color: #895124;
        text-align: center;
        font-size: 20px;
        line-height: 26px;
        margin-top: 20px;
        width: 100%;
      }

      .dialog-btn:hover {
        background: #fff5df;
        color: #426308;
      }

      .dialog-pane img.thanks-hero {
        margin-top: 10px;
        border: 1px solid #cbb572;
      }

      /* form errors */

        form input[type=text],
        form input[type=password],
        form input[type=email],
        form select,
        form textarea,
        {
          box-sizing: border-box;
          width: 100%;
          height: auto;
        }
        form input.error,
        form input.errors,
        form textarea.error,
        form textarea.errors
        {
          border: 1px solid darkred;
        }
        form.small {
          width: 33%;
        }
        form.medium {
          width: 66%;
        }
        .errors-summary {
          box-sizing: border-box;
          width: 85%;
          height: auto;
          color: #666;
          background-color: #fffff6;
          background-color: #fff;
          border: 1px solid darkred;
          padding: 1em;
          margin: 1em auto;
        }
        .errors-summary ul {
        }
        .errors-caption {
          font-weight: bold;
        }
        .errors-title {
          float: left;
        }
        .errors-title:after {
        content: "\a0 ";
        }
        .errors-message {
          color: red;
        }
        label[data-required]:after {
          content : ' *';
        }
        label {
          font-weight: 500;
          color: #666;
        }

      /* font-awesome */
      .fa-location-arrow:hover {
        cursor: pointer;
      }



      /* bootstrap width overrides for FB 810px width */

      @media (min-width: 768px) and (max-width: 979px) {
        .container {
          width: 810px;
        }

        .span12 {
          width: 810px;
        }

      }

    </style>

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="bootstrap/js/html5shiv.js"></script>
    <![endif]-->

    <!-- Fav and touch icons -->
    <link rel="shortcut icon" type="image/x-icon" href="favicon.ico?v=2">

    <%= javascript_include_tag 'geoPosition', 'locator' %>
    <%= javascript_tag "window.brand = '#{ @brand.slug }'", defer: 'defer' %>
    <%= stylesheet_link_tag("locator") %>
  </head>

  <body>
    <div class="container">
      <header>
        <%= image_tag "#{ @brand.slug }/logo.png", :style => 'max-width:150;max-height:150px;' %>
      </header>
      
      <div class="row-fluid">
        <div class="span12">
          <h2 id="locatorHeader" class="text-center">
            Thirsty? Find <%= @brand.title %> below.
          </h2>
        </div>
      </div>

      <div class="row-fluid">
        <div class="span12">
          <div id='beer-locator-form'>
            <%= render :partial => 'organizations/paulaner/form' %>
          </div>
      	  <div id="locations">
            <div id="locator_message" class="alert" style="display: none;"></div>
            <div id="location_results"></div>
      	  </div>
        </div>
      </div>

      <div class="row-fluid">
        <footer class="span12"></footer>
      </div>

    </div>

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
    <script src='/assets/application.js'></script>
    <script src="js/nav.js"></script>
    <script>
      jQuery(function(){
        //Brand.locator_form("hacker-pschorr", "#beer-locator-form");
      });
    </script>

    <div id="fb-root"></div>
    <script>(function(d, s, id) {
      var js, fjs = d.getElementsByTagName(s)[0];
      if (d.getElementById(id)) return;
      js = d.createElement(s); js.id = id;
      js.src = "https://connect.facebook.net/en_US/all.js#xfbml=1&appId=497149513650330";
      fjs.parentNode.insertBefore(js, fjs);
      }(document, 'script', 'facebook-jssdk'));</script>
  </body>
</html>
