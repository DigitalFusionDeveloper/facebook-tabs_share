
<br>

<%=

  index =
    Dir.glob(Rails.root.join('public', 'brands', '*').to_s).
      select{|entry| test(?d, entry)}.
        map{|entry| File.basename(entry)}.
          sort

  ul_(:class => ''){ index.each{|brand| li_{ a_(:href => "./brands/#{ brand }", :target => :_blank){ brand } } } }

%>

