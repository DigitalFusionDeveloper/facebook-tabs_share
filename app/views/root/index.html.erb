<br>

<%=

  dl_{

    Brand.all.each do |brand|

      dt_{ brand.title }

      [brand.index_url, brand.rfi_url, brand.locator_url].each do |url|
        if url 
          dd_{
            a_(:href => "#{ url }", :target => :_blank){ url }
          }
        end
      end
    end

  }

%>

